#include "variadic_functions.h"
#include <stddef.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	print_all("ceissfcs", 'B', 3, "stSchool", "yoyo", 0.65566, 'a', NULL);
	return (0);
}