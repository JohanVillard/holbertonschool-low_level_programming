#include "main.h"

/**
* set_string - change the direction to an other array
* @s: double pointer which we want change the direction
* @to: pointer which have the good direction
*
* Return: (0)
*/
void set_string(char **s, char *to)
{
*s = to;
}
